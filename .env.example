# Application Configuration
# This file contains non-secret default values only.
# SECRETS MUST be injected via Docker/CI environment variables in production.

# Database Configuration
# Format: postgresql+asyncpg://user:password@host:port/dbname
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/interview_db

# Individual PostgreSQL settings (optional if DATABASE_URL is provided)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=interview_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Application Settings
DEBUG=True
LOG_LEVEL=INFO
API_V1_PREFIX=/api/v1
HOST=0.0.0.0
PORT=8000

# SECRETS - These should NOT be committed to version control
# In production, inject these via Docker/CI environment variables
# Example: docker run -e SECRET_KEY=your-actual-secret-key ...
SECRET_KEY=your-secret-key-change-in-production
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-2.0-flash-exp

# Note: For production deployments:
# - Never commit actual secrets to .env files
# - Use Docker secrets, Kubernetes secrets, or CI/CD secret management
# - Override these values via environment variables in your deployment
